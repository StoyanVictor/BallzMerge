using UnityEngine;

public class BlockSpawner : MonoBehaviour
{
    [SerializeField] private GameObject block;
    private void Start()
    {
        // var blockPosition = _gridManager.GetCellCenter(1, 1);
        // var blockPosition1 = _gridManager.GetCellCenter(2, 3);
        // var blockPosition2 = _gridManager.GetCellCenter(1, 3);
        // var blockPosition3 = _gridManager.GetCellCenter(0, 1);
        // var blockPosition4 = _gridManager.GetCellCenter(0, 0);
        // Instantiate(block, blockPosition, new Quaternion());
        // Instantiate(block, blockPosition1, new Quaternion());
        // Instantiate(block, blockPosition2, new Quaternion());
        // Instantiate(block, blockPosition3, new Quaternion());
        // Instantiate(block, blockPosition4, new Quaternion());
    }
}